{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "docs",
  "description": "The documentation window: it only needs to be able to close itself",
  "windows": ["docs"],
  "permissions": ["core:default", "core:window:allow-close", "opener:default"]
}
